[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "numeric-base-lib"
version = "0.1.0"
description = "Convert float numbers to arbitrary base (2-36)"
license = "MIT"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
